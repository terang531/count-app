# AGENTS.md

## このプロジェクトについて
- `test-app-1`は、押すたびに数字が増えるカウンターアプリ（Firebase/Firestore使用、Netlifyで公開）。
- 目的は、アプリの機能そのものではなく、**Gitの運用の型（ブランチ→PR→プレビュー確認→マージ）を体験・練習すること**。
- 開発者（私）はプログラミング初心者。専門用語を使う場合は、簡単な説明を添えてください。
- これは練習用の使い捨てアプリであり、デザインや機能の作り込みは不要。

## Gitの運用ルール（今回の練習で最も重要な点）
- `main`ブランチには直接コミットしないこと。必ず作業用のブランチを作ってから変更を始めること。
- 変更がひと通り終わったら、Pull Requestを作成すること。
- **Pull Requestを作成したら、そこで一度止まること。** 私がNetlifyのプレビュー画面を確認し、明示的に指示するまで、`main`へのマージは行わないこと。
- 次の機能に進む前に、必ず前の機能のPRがマージされるのを待つこと。複数の機能を同時並行で進めないこと。

## 今回追加したい機能（1つずつ、別のブランチ・別のPRで）
1. カウンターを0に戻す「リセット」ボタン
2. 数字が10の倍数になったら、表示の色が変わる
3. 「最後に押された時刻」を画面に表示する

## Netlifyのクレジットについて（注意）
- Netlifyの無料プランには「クレジット」という消費上限があり、本番環境への公開（mainへのマージ後の自動デプロイ）のたびに消費される。
- そのため、細かい微調整のたびにmainへマージ・本番公開することは避けること。プレビュー環境での確認を優先し、本番への反映は意味のあるまとまりごとに行うこと。
